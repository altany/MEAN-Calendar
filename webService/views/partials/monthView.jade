div.currentMonth {{nameOfMonth}}

div.calendar.monthView
    div(ng-repeat='i in getNumber(totalDays) track by $index')
        <day-of-month date='{{yearMonth}}{{twoDigit(1+$index)}}'></day-of-month>